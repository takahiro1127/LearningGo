#include <iostream>
#include <algorithm>
#include <string>
#include <map> // ヘッダファイルインクルード
#define rep(i, n) for (int i = 0; i < (n); ++i)
using namespace std;
typedef long long ll;
// 1.6
int main()
{
    // 前から走査していく。
    // 次々とvectorに追加、もしくは、stringに追加していく。
    // 元の文字列の長さを超えた段階で元の文字列を返す。
}
