#include <iostream>
#include <algorithm>
#include <string>
#include <map> // ヘッダファイルインクルード
#define rep(i, n) for (int i = 0; i < (n); ++i)
using namespace std;
typedef long long ll;
// 1.7
int main()
{
    // n*mの行列を90度回転させる
    // これを追加の領域なしで
    // n > mとした場合、n*nの領域があったとする。
    // (n, n)より上側の三角行列に対して、swapを繰り返す
}
