#include <iostream>
#include <algorithm>
#include <string>
#include <map> // ヘッダファイルインクルード
#define rep(i, n) for (int i = 0; i < (n); ++i)
using namespace std;
typedef long long ll;
// 2.2
int main()
{
    // 先頭からn-k番目を探すアルゴリズムにすればいい
    // 長さがわからない場合、stackにポインタ変数を積んで行けばいい
}