#include <iostream>
#include <algorithm>
#include <string>
#include <map> // ヘッダファイルインクルード
#define rep(i, n) for (int i = 0; i < (n); ++i)
using namespace std;
typedef long long ll;
// 2.5
int main()
{
    // 後ろからpush backする
    // 偶奇で話は変わらない
    // なぜなら、奇数の場合同時に同じ場所を参照するだけであるから
}