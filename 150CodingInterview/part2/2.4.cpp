#include <iostream>
#include <algorithm>
#include <string>
#include <map> // ヘッダファイルインクルード
#define rep(i, n) for (int i = 0; i < (n); ++i)
using namespace std;
typedef long long ll;
// 2.2
int main()
{
    // 後ろから走査して、xより大きいものはpush frontしてeraseすればいい
}