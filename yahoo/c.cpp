#include<iostream>
#include <algorithm>
using namespace std;
typedef unsigned long ll;
#define rep(i, n) for(int i = 0; i < (n); i++)
int main() {
    ll n, h, r;
    cin >> n >> h >> r;
    ll a[n], c[n];//
    rep(i, n) cin >> a[i] >> c[i];
    ll cost = c[0];
    
}
