//n頭の牛　各牛は前か後ろ向き
// ジョンはすべての牛を前向きにしたい→牛回転機
//購入時に数字Kを設定→一度に連続するKひき回転できる
//最小の操作回数Mとそれを達成する最小のM
#include<iostream>
using namespace std;
int N;
int main() {
    cin >> N;
    int a[N];
    for (int i = 0; i < N; i++) {
        string d;
        cin >> d;
        if (d == "F") {
            a[i] = 0;
        } else {
            a[i] = 1;
        }
    }

    for () {
        for () {

        }
    }
}

//ループn^3はいけるけど、それをn^2に抑えるための式のすごさがよくわからなかった。
