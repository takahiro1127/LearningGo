import pandas as pd
import numpy as np
import os

# ここは適宜指定。
df = pd.read_csv(os.getcwd() + '/output_' + index + '.csv')
def cost(df):
    df.groupby(['', ''], axis=1).groupby('')
def sale(df):

def income(df):
