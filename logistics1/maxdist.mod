### ルート生成パラメーター

let Max_Neibor := 15;   #===> SCからルート上のＪＰサイトまでの最大距離、5 ～ 30 の間で変更する。Small=30, Medium=15, large=8
let avg_speed := 30;
let wrk_hours := 16;
let big_capac := 800;
let small_capac := 500;
let big_cost := 100;
let small_cost := 80;
let Fix_Cost := 333.3;
let Max_SC := 27;
